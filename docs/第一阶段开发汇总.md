# 刑事案件 RAG 检索系统 - 第一阶段开发汇总

**版本**: 1.0.0
**日期**: 2026-02-17
**基于**: RAGFlow 开源项目二次开发

---

## 一、项目概述

本项目基于 [RAGFlow](https://github.com/infiniflow/ragflow) 进行二次开发，构建面向刑事案件扫描版案卷 PDF 的"可检索、可追溯引用、可强制校验"的 RAG 问答系统。

### 核心目标

- 在大量案卷中快速定位与问题相关的原文证据
- 输出结构化答案，并提供可核验引用（页码 + 坐标 + 摘录）
- 避免模型编造：无证据则明确"材料未显示"

### 目标用户

| 用户 | 场景 | 核心诉求 |
|---|---|---|
| 律师 | 案件研究、会见/庭前准备 | 快速找到可引用原文证据 |
| 检察官 | 证据核查、审查起诉 | 证据定位 + 结论可核验 |
| 律师团队 | 协作办案 | 统一引用口径、减少遗漏 |

---

## 二、第一阶段完成的 PR

### PR-1: Schema 扩展 (Chunk Schema Extension)

**状态**: ✅ 已完成

**目标**: 扩展 chunk 元数据，支持精确定位和引用

**实现内容**:
- 在 ES schema 添加字段：`block_refs: JSON`, `bbox_union: ARRAY(Integer)`
- 在 `rag/nlp/__init__.py` 的 `add_positions()` 函数中添加新字段处理
- 在 `rag/svr/task_executor.py` 的 `insert_chunks()` 中存储新字段

**代码改动**:
```
rag/nlp/__init__.py           # 扩展 add_positions
rag/svr/task_executor.py      # 存储新字段
```

---

### PR-2: 起诉意见书 Chunker (Indictment Chunker)

**状态**: ✅ 已完成

**目标**: 实现起诉意见书的结构化分块

**实现内容**:
- 创建 `rag/app/indictment.py`（参考 `rag/app/interrogation.py`）
- 定义 section 触发词列表（如"经依法侦查查明"、"认定上述犯罪事实的证据如下"等）
- 按 section 切分，段落超过 800 字自动分割
- 计算 bbox_union 和 block_refs 用于精确定位

**代码改动**:
```
rag/app/indictment.py         # 新增：起诉意见书 Chunker
rag/svr/task_executor.py      # 注册新 chunker
```

---

### PR-3: Answer Gate 校验器

**状态**: ✅ 已完成

**目标**: 强制校验 LLM 输出，防止幻觉和编造

**实现内容**:
- 创建 `rag/answer_gate/validator.py`
- 实现 chunk_id 存在性校验
- 实现 excerpt 子串匹配校验
- 实现数值/日期严格落地校验（正则 + 子串匹配）
- 实现 page_index/bbox 来源校验
- 实现无证据返回逻辑（status="no_evidence"）

**校验规则**:
1. `chunk_id` 必须存在于检索结果或库中
2. `excerpt` 必须为 `chunk.text` 的子串
3. 结论中的金额/日期/浓度等数值必须在 excerpt 中逐字出现
4. `page_index/bbox` 必须来自 chunk 元数据

**代码改动**:
```
rag/answer_gate/validator.py  # 新增：引用校验器
api/db/services/dialog_service.py  # 集成 Answer Gate
```

---

### PR-4: 检索扩展 (Retrieval Extension)

**状态**: ✅ 已完成

**目标**: 扩展检索 API 返回 block_refs 支持前端精确定位

**实现内容**:
- 在 `rag/nlp/search.py:Dealer.retrieval()` 返回值中添加 block_refs
- 在 `api/apps/chunk_app.py` 的 `/list` 响应中包含 block_refs
- 添加 doc_type 过滤支持
- 确保 raw_chunks 在响应中返回

**代码改动**:
```
rag/nlp/search.py             # 返回 block_refs
api/apps/chunk_app.py         # API 响应扩展
```

---

### PR-5: 集成测试 (E2E Integration)

**状态**: ⏳ 进行中

**目标**: 端到端测试验证整体功能

**实现内容**:
- 端到端测试：上传 → 解析 → 检索 → 问答 → 校验引用
- 更新 API 文档

---

### PR-6: PaddleVL 作为默认 PDF 解析器

**状态**: ✅ 已完成

**目标**: 将 PaddleVL (PaddleOCR-VL) 设为默认 PDF 解析器，提升法律文档处理效果

**实现内容**:
- 修改 `api/utils/api_utils.py`: `layout_recognize` 默认值从 `"DeepDOC"` 改为 `"PaddleOCR-VL@paddleocr"`
- 修改 `rag/flow/parser/parser.py`: `parse_method` 默认值从 `"deepdoc"` 改为 `"paddleocr"`
- 更新测试配置

**代码改动**:
```
api/utils/api_utils.py        # 修改默认 layout_recognize
rag/flow/parser/parser.py     # 修改默认 parse_method
test/testcases/configs.py     # 更新测试配置
```

---

## 三、进行中的变更

### 自动检测文书类型解析方案 (Auto-detect Document Parser)

**状态**: 🚧 开发中

**目标**: 移除知识库创建时的解析方法选择，改为上传文件时自动识别文书类型

**计划内容**:
- 移除知识库创建对话框中的解析方法选择 UI
- 新增文书类型自动识别服务
- 支持的法律文书类型：讯问笔录、起诉意见书、其他法律文书

**计划代码改动**:
```
web/src/pages/datasets/dataset-creating-dialog.tsx  # 简化 UI
api/apps/kb_app.py                                  # 修改接口参数
rag/app/classifier.py                               # 新增：文书类型分类器
```

---

### Benchmark 检索测试 (Retrieval Benchmark)

**状态**: 🚧 开发中

**目标**: 建立系统性测试来验证检索效果

**计划内容**:
- 自动化检索测试脚本
- 测试报告生成
- 测试指标：事实型题目召回率、证据集合型题目完整性、冲突缺口型题目诚实性

**已有题库**:
- `benchmark/起诉意见书/曾庆成危险驾驶案/` - 20 道测试题
- `benchmark/讯问笔录/陈明飞诈骗案/` - 25 道测试题

---

## 四、系统架构

```
┌─────────────────────────────────────────────────────────────┐
│                     刑事案件 RAG 系统架构                      │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌─────────────┐    ┌─────────────┐    ┌─────────────┐     │
│  │ 扫描版 PDF  │ -> │ PaddleVL   │ -> │ Block 入库  │     │
│  │ 案卷文件    │    │ OCR 解析    │    │ (带坐标)    │     │
│  └─────────────┘    └─────────────┘    └─────────────┘     │
│                                                │            │
│                     ┌──────────────────────────┘            │
│                     ▼                                       │
│  ┌─────────────┐    ┌─────────────┐    ┌─────────────┐     │
│  │ 起诉意见书  │    │ 讯问笔录    │    │ 索引构建    │     │
│  │ Chunker    │    │ Chunker    │    │ (向量+BM25) │     │
│  └─────────────┘    └─────────────┘    └─────────────┘     │
│                                                │            │
│                     ┌──────────────────────────┘            │
│                     ▼                                       │
│  ┌─────────────┐    ┌─────────────┐    ┌─────────────┐     │
│  │ 用户问题    │ -> │ 混合检索    │ -> │ LLM 生成    │     │
│  └─────────────┘    └─────────────┘    └─────────────┘     │
│                                                │            │
│                     ┌──────────────────────────┘            │
│                     ▼                                       │
│  ┌─────────────┐    ┌─────────────┐                         │
│  │ Answer Gate │ -> │ 结构化答案  │                         │
│  │ 强制校验    │    │ + 可追溯引用│                         │
│  └─────────────┘    └─────────────┘                         │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

## 五、核心数据结构

### Block Schema（原始事实层）

```json
{
  "case_id": "string",
  "doc_id": "string",
  "doc_type": "indictment_opinion | interrogation_record",
  "page_index": 1,
  "page_width": 0,
  "page_height": 0,
  "block_id": 0,
  "block_order": 0,
  "label": "text | number | header | footer | other",
  "text": "string",
  "bbox": [0, 0, 0, 0],
  "polygon": [[0,0],[0,0],[0,0],[0,0]]
}
```

### Chunk Schema（检索单位层）

```json
{
  "case_id": "string",
  "doc_id": "string",
  "doc_type": "indictment_opinion | interrogation_record",
  "chunk_id": "string",
  "chunk_type": "qa_pair | paragraph | evidence_item | section",
  "text": "string",
  "page_range": [1, 2],
  "bbox_union": [0,0,0,0],
  "block_refs": [{"page_index":1,"block_id":2},{"page_index":1,"block_id":3}],
  "anchors": {
    "text_hash": "string",
    "excerpt_head": "string"
  }
}
```

### Answer Schema（问答输出）

```json
{
  "status": "ok | no_evidence | citation_insufficient",
  "conclusion": "string | null",
  "evidences": [
    {
      "chunk_id": "string",
      "page_index": 1,
      "bbox": [0,0,0,0],
      "excerpt": "string"
    }
  ],
  "reasoning": "string",
  "gaps": ["string"],
  "raw_chunks": [...]
}
```

---

## 六、验收指标

| 指标 | 阈值 | 定义 |
|---|---|---|
| Citation Precision | ≥ 98% | 引用摘录必须包含支撑该结论的事实/数值 |
| Numeric/Date Grounding | = 100% | 答案中数值必须在证据摘录中逐字出现 |
| Retrieval Recall@20 | ≥ 95% | 金标问题的关键证据 chunk 在 Top-20 检索结果中出现 |
| OOD "材料未显示" 触发正确率 | ≥ 95% | 对题库外问题应答为"材料未显示/引用不足" |

---

## 七、技术栈

### 后端
- Python 3.10-3.12
- Flask (API 服务器)
- Elasticsearch / Infinity (向量+关键词索引)
- PaddleOCR-VL (PDF OCR + 版面分析)

### 前端
- React + TypeScript
- UmiJS 框架
- Ant Design + shadcn/ui 组件

### 部署
- Docker + Docker Compose
- MinIO (对象存储)
- MySQL (关系数据)
- Redis (缓存)

---

## 八、后续规划

### Phase 2 (计划中)
- 自动检测文书类型解析方案完善
- Benchmark 检索测试完善
- E2E 集成测试完善

### Phase 3 (规划中)
- 时间线分析
- 证据链构建
- 矛盾点检测
- 多轮对话支持

---

## 九、贡献者

本项目基于 RAGFlow 开源项目进行二次开发，感谢 RAGFlow 团队的优秀工作。

---

## 十、许可证

继承 RAGFlow 的 Apache-2.0 许可证。
